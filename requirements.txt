# Core runtime dependencies for the Ignis Sport Europe Hunter.
httpx>=0.27
beautifulsoup4>=4.12
lxml>=5.0
pydantic>=2.6
pydantic-settings>=2.2
SQLAlchemy>=2.0
fastapi>=0.110
uvicorn[standard]>=0.29
python-multipart>=0.0.9
APScheduler>=3.10
feedparser>=6.0
Jinja2>=3.1
python-dotenv>=1.0

# Optional but used when available:
anthropic>=0.40        # Claude intelligence layer (degrades gracefully)
Pillow>=10.0           # image hashing
ImageHash>=4.3         # perceptual hashing for cross-platform dedup

# Optional browser automation for JS-heavy sources (install separately):
# playwright>=1.42
